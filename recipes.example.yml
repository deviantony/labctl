# recipe file (draft)
recipes:
  portainer-server:
    # the base image to use
    base: ubuntu-2204
    # region: "-"
    # size: "-"
    # the recipe steps to follow
    steps:
      - curl https://anthony.portainer.io/docker-portainer | bash
      - ... # any other step to add / automate

  portainer-device:
    base: ubuntu-2204
    # the files that must be packaged with the recipe
    files:
      - /etc/portainer-agent
      - /var/lib/portainer-agent
    steps:
      - curl https://anthony.portainer.io/docker-portainer | bash
      - ... # any other step to add / automate

  microk8s-cluster:
    flasks:
      manager:
        base: ubuntu-2204
        steps:
          - curl https://anthony.portainer.io/k8s-master | bash
          - ... # any other step to add / automate
      worker1:
        base: ubuntu-2204
        steps:
          - curl https://anthony.portainer.io/k8s-master | bash
          - ... # any other step to add / automate
      worker2:
        base: ubuntu-2204
        steps:
          - curl https://anthony.portainer.io/k8s-master | bash
          - ... # any other step to add / automate